from models.Person import *
#Controlador para agregar una nueva persona a la base de datos.
def insert(key,name,age):
    if (key != '') and (name != '') and (age != ''):
        #persona  = Person(None)
        #persona.setValueColumn('keyStr',key)
        #persona.setValueColumn('name',name)
        #persona.setValueColumn('age',age)
        #persona.save()
        pass
